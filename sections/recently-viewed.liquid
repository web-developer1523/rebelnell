{% render 'section-recently-viewed',
  section: section,
  recently_viewed_max_products: settings.recently_viewed_max_products,
  product: product,
  hydration: 'on:visible'
%}

{% schema %}
{
  "name": "t:sections.recently-viewed.name",
  "class": "recently-viewed__section",
  "settings": [
    {
      "type": "paragraph",
      "content": "t:sections.recently-viewed.settings.content"
    }
  ],
  "disabled_on": {
    "groups": ["footer", "header", "custom.popups"]
  }
}
{% endschema %}
